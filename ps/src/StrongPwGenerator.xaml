<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Strong Password Generator"
        WindowStyle="None"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource OuterBoxBrush}"
        Width="700" Height="750">
    <Window.Resources>
        <!-- Theme brushes -->
            <SolidColorBrush x:Key="ScrollBarThumbBrush" Color="#FF4D4D4D"/>
            <SolidColorBrush x:Key="ScrollBarThumbHoverBrush" Color="#FF5A5A5A"/>
            <SolidColorBrush x:Key="ScrollBarTrackBrush" Color="#FF3B3B3B"/>
            <SolidColorBrush x:Key="WindowButtonHoverBrush" Color="#FF682574"/>
            <SolidColorBrush x:Key="TextBrush" Color="#FFCCCCCC"/>
            <SolidColorBrush x:Key="ButtonTextBrush" Color="#FFCCCCCC"/>
            <SolidColorBrush x:Key="BorderBrush" Color="#FF636363"/>
            <SolidColorBrush x:Key="BackgroundBrush" Color="#FF2D2D2D"/>
            <SolidColorBrush x:Key="ButtonBrush" Color="#FF4D4D4D"/>
            <SolidColorBrush x:Key="HoverBrush" Color="#FF5A5A5A"/>
            <SolidColorBrush x:Key="OuterBoxBrush" Color="#FF3B3B3B"/>
            <SolidColorBrush x:Key="SelectedBrush" Color="#FF4D4D4D"/>
            <SolidColorBrush x:Key="TitleBarBrush" Color="#FF853993"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF5A5A5A"/>
            <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF2D2D2D"/>

            <CornerRadius x:Key="CornerRadiusValue">6</CornerRadius>
            <Thickness x:Key="BorderThicknessValue">1</Thickness>
            <FontWeight x:Key="TitleFontWeightValue">Bold</FontWeight>

        <!-- Implicit Thumb style -->
        <Style TargetType="Thumb">
            <Setter Property="Background" Value="{StaticResource ScrollBarThumbBrush}"/>
            <Setter Property="Margin" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="ThumbBorder" Background="{TemplateBinding Background}" CornerRadius="2" SnapsToDevicePixels="True"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{StaticResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ScrollBar styling using theme brushes -->
        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="{StaticResource ScrollBarTrackBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>

        <!-- RepeatButton style for ScrollBar arrows/tracks -->
        <Style TargetType="RepeatButton">
            <Setter Property="Background" Value="{StaticResource ScrollBarTrackBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonTextBrush}"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="{StaticResource ScrollBarThumbHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowControlButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="40"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource WindowButtonHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="4"/>
            <Setter Property="SelectionBrush" Value="{StaticResource SelectedBrush}"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ButtonBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource ButtonTextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="{StaticResource BorderThicknessValue}"/>
            <Setter Property="Padding" Value="6,2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{StaticResource CornerRadiusValue}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton"
                                          Grid.Column="2"
                                          Focusable="false"
                                          IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press"
                                          Background="{StaticResource BackgroundBrush}"
                                          BorderBrush="{StaticResource BorderBrush}"
                                          BorderThickness="1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition/>
                                        <ColumnDefinition Width="20"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter Grid.Column="0"
                                                      Name="ContentSite"
                                                      IsHitTestVisible="False"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                      ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                                      Margin="5,3,5,3"
                                                      VerticalAlignment="Center"
                                                      HorizontalAlignment="Left"
                                                      TextBlock.Foreground="{StaticResource TextBrush}"/>
                                    <TextBlock Grid.Column="1" 
                                               Text="▼" 
                                               FontSize="8"
                                               Foreground="{StaticResource TextBrush}"
                                               VerticalAlignment="Center" 
                                               HorizontalAlignment="Center"/>
                                </Grid>
                            </ToggleButton>
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource BackgroundBrush}"
                                            BorderThickness="1"
                                            BorderBrush="{StaticResource BorderBrush}"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" CanContentScroll="False">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="5,3"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Border" 
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                SnapsToDevicePixels="True">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource SelectedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource OuterBoxBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel x:Name="TitleBar" Grid.Row="0" Background="{StaticResource TitleBarBrush}" LastChildFill="True">
            <Image x:Name="AppIcon" Width="24" Height="24" Margin="10,0,0,0" VerticalAlignment="Center" DockPanel.Dock="Left"/>
            <TextBlock Text="Strong Password Generator" Foreground="{StaticResource TextBrush}" FontWeight="{StaticResource TitleFontWeightValue}" FontSize="14" VerticalAlignment="Center" Margin="6,0,0,0" DockPanel.Dock="Left"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="MinimizeButton" Style="{StaticResource WindowControlButtonStyle}" Content="—" FontSize="16"/>
                <Button x:Name="MaximizeButton" Style="{StaticResource WindowControlButtonStyle}" Content="□" FontSize="16"/>
                <Button x:Name="CloseButton" Style="{StaticResource WindowControlButtonStyle}" Content="✕" FontSize="16"/>
            </StackPanel>
        </DockPanel>

        <Border Grid.Row="1" Background="{StaticResource BackgroundBrush}" CornerRadius="{StaticResource CornerRadiusValue}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="{StaticResource BorderThicknessValue}" Margin="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" CanContentScroll="False" PanningMode="Both">
                <Grid>
                    <StackPanel Margin="15">
                        <TextBlock Text="Strong Password Generator" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,10,0,5"/>
                        <TextBlock Text="Generated password - right click and copy:" Foreground="{StaticResource TextBrush}" FontStyle="Italic" Margin="0,0,0,5"/>

                        <TextBox x:Name="PasswordOutput" FontSize="16" FontWeight="Bold" IsReadOnly="True" TextWrapping="Wrap" MinHeight="60" Margin="0,0,0,5"/>

                        <TextBlock x:Name="CombinationsText" Text="Possible combinations: " Foreground="{StaticResource TextBrush}" FontWeight="Bold" FontStyle="Italic" Margin="0,0,0,10"/>

                        <TextBlock TextWrapping="Wrap" Foreground="#FF6B6B" Margin="0,0,0,10">
                            <Run FontWeight="Bold">Note:</Run>
                            <Run> It is possible to generate passwords without all checked elements. If this happens, try to regenerate. Counter cannot exceed 1.7976931348623157e+308.</Run>
                        </TextBlock>

                        <Separator Margin="0,5,0,10" Background="{StaticResource BorderBrush}" Height="1"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <TextBlock Text="Length: " Foreground="{StaticResource TextBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <ComboBox x:Name="LengthSelector" Width="80" Height="25" SelectedIndex="12"/>
                            <Button x:Name="GenerateButton" Content="Generate" Width="100" Height="30" Margin="20,0,0,0"/>
                            <Button x:Name="CopyButton" Content="Copy Password" Width="120" Height="30" Margin="10,0,0,0"/>
                        </StackPanel>
                        <TextBlock x:Name="StatusText" Foreground="#FF6B6B" FontStyle="Italic" Margin="0,0,0,6" TextWrapping="Wrap"/>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Opacity="0.85">
                            <Run FontWeight="Bold">Security:</Run>
                            <Run> Passwords draw entropy from the operating system's cryptographic random number generator.</Run>
                        </TextBlock>

                        <Separator Margin="0,5,0,10" Background="{StaticResource BorderBrush}" Height="1"/>

                        <TextBlock Text="Select Character Sets:" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,5"/>

                        <CheckBox x:Name="LatinCheck" Content="Latin: ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz" IsChecked="True" Margin="0,3"/>
                        <CheckBox x:Name="NumbersCheck" Content="Numbers: 0123456789" IsChecked="True" Margin="0,3"/>
                        <CheckBox x:Name="SignsCheck" Content="Signs: /|()1{}[]?-_+~&lt;&gt;!;:,^'.$@%&amp;#*" IsChecked="True" Margin="0,3"/>
                        <CheckBox x:Name="NordicCheck" Content="Nordic: ÁáÐðÉéÍíÓóÚúÝýÞþÆæÖöZzÄäØøÅå" Margin="0,3"/>
                        <CheckBox x:Name="GreekCheck" Content="Greek: ΑαΒβΓγΔδΕεΖζΗηΘθΙιΚκΛλΜμΝνΞξΟοΠπΡρΣσςΤτΥυΦφΧχΨψΩω" Margin="0,3"/>
                        <CheckBox x:Name="CyrillicCheck" Content="Cyrillic: БбВвГгДдЕеЖжЗзИиЙйКкЛлМмНнОоПпРрСсТтУуФфХхЦцЧчШшЩщЬьЮюЯя" Margin="0,3"/>
                        <CheckBox x:Name="ArmenianCheck" Content="Armenian: աբգդեզէըթժիլխծկհձղճմյնշոչպջռսվտրցւփքօֆուև" Margin="0,3"/>
                        <CheckBox x:Name="HangulCheck" Content="Hangul: ㄱㄴㄷㄹㅁㅂㅅㅇㅈㅊㅋㅌㅍㅎㅏㅑㅓㅕㅗㅛㅜㅠㅡㅣ" Margin="0,3"/>
                        <CheckBox x:Name="ArabicCheck" Content="Arabic: ءي و ه ن م لك ق ف غ ع ظ ط ض ص ش س ز ر ذ د خ ح ج ث ت ب ا" Margin="0,3"/>
                        <CheckBox x:Name="GeorgianCheck" Content="Georgian: აბგდევზთიკლმნოპჟრსტუფქღყშჩცძწჭხჯჰ" Margin="0,3"/>
                        <CheckBox x:Name="EthiopianCheck" Content="Ethiopian: ሀለሐመሠረሰቀበተኀነአከወዐዘየደገጠጸፀፈጰፐ" Margin="0,3"/>
                        <CheckBox x:Name="ThaanaCheck" Content="Thaana: ހށނރބޅކއވމފދތލގސޑޒޓޔޕޖޗޘޙޚޛޜޝޞޟޠޡޢޣޤޥ       ޱ" Margin="0,3"/>
                        <CheckBox x:Name="HanziCheck" Content="Hanzi: ぁあぃいぅうぇえぉおかがきぎくぐけげこごさざしじすずせぜそぞただちぢっつづてでとどなにぬねのはばぱひびぴふぶぷへべぺほぼぽまみむめもゃやゅゆょよらりるれろゎわゐゑをんゔゕゖ  ゝゞゟ" Margin="0,3"/>

                        <Separator Margin="0,10,0,10" Background="{StaticResource BorderBrush}" Height="1"/>

                        <TextBlock Text="Guide:" FontSize="14" FontWeight="Bold" Foreground="{StaticResource TextBrush}" Margin="0,0,0,5"/>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,3"> Do not Save passwords in your browser</TextBlock>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,3"> Consider password managers like KeePass</TextBlock>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,3"> Do NOT reuse passwords</TextBlock>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,3"> Use 2-factor authentication when possible</TextBlock>
                        <TextBlock TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" Margin="0,3"> Avoid dictionary words</TextBlock>

                        <Separator Margin="0,10,0,5" Background="{StaticResource BorderBrush}" Height="1"/>

                        <TextBlock Foreground="{StaticResource TextBrush}" Margin="0,5" TextWrapping="Wrap" Text=" 2007-2025, Seahawk.dk - MIT License - GitHub: OZoneSQT/StrongPasswordGenerator"/>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
